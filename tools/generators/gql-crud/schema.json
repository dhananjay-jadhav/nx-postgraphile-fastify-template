{
    "$schema": "https://json-schema.org/schema",
    "$id": "WrapLib",
    "title": "Create WrapPlans Library",
    "description": "Generate a new wrapPlans library for a GraphQL type",
    "type": "object",
    "properties": {
        "typeName": {
            "type": "string",
            "description": "The GraphQL type name (e.g., User, Post, Comment)",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "What is the GraphQL type name? (e.g., User, Post)"
        },
        "name": {
            "type": "string",
            "description": "Custom library name (default: lowercase plural of typeName)",
            "x-prompt": "Custom library name? (leave empty for default plural)"
        },
        "directory": {
            "type": "string",
            "description": "Directory where the library will be created",
            "default": "libs"
        }
    },
    "required": ["typeName"]
}
